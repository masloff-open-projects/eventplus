#!/usr/bin/env node

/**
 * Driver to work with socket connection
 *
 * @version 1.0.0
 */

module.exports = function (http) {

    var io = new (require('ws')).Server({ port: 9999 });
    return io;

}